{
  "name": "@zeos/mcp-servers",
  "version": "0.1.0",
  "private": true,
  "description": "MCP server infrastructure for zeos - local-first operation with async GitHub sync",
  "author": "my-org <support@my-org.com>",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/rgsuarez/zeos.git",
    "directory": "infrastructure/mcp-servers"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=8.0.0"
  },
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "build": "pnpm -r build",
    "clean": "pnpm -r clean",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "dev": "pnpm --filter @zeos/mcp-server dev"
  },
  "devDependencies": {
    "@anthropic-ai/sdk": "^0.50.0",
    "@modelcontextprotocol/sdk": "^1.0.0",
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0",
    "vitest": "^2.0.0",
    "tsup": "^8.0.0"
  }
}
